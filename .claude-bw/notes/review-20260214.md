**1) Critical Bugs & Logic Errors**

1. Severity: **Critical**  
   File & line: `src/Api/Endpoints/ReviewsEndpoint.php:70`, `src/Api/Endpoints/ReviewsEndpoint.php:93`, `src/Api/Endpoints/ReviewsEndpoint.php:168`, `src/Api/Endpoints/ReviewsEndpoint.php:548`, `src/Review/ReviewManager.php:462`  
   What’s wrong: review read endpoints are public, allow `status=pending|all`, and single-review fetch does not enforce approved-only status.  
   Why it matters: unauthenticated users can read non-approved reviews (moderation/privacy leak).  
   Suggested fix: in public reads, hard-force status to approved; allow pending/all only for admins/review author. Add explicit status check in `get_review()` before returning data.
   Status: **Fixed** (2026-02-14).  
   Resolution: implemented public-read status enforcement (`approved` for non-admins) and added a `get_review()` visibility check so non-approved reviews are only visible to admins or the review author (`src/Api/Endpoints/ReviewsEndpoint.php`).

2. Severity: **High**  
   File & line: `src/Api/Endpoints/ReviewsEndpoint.php:456`, `src/Api/Endpoints/ReviewsEndpoint.php:463`, `src/Review/ReviewManager.php:492`, `src/Review/ReviewManager.php:501`  
   What’s wrong: endpoint passes `page/per_page`, manager expects `number/offset`; pagination args are ignored.  
   Why it matters: API pagination is incorrect at runtime (wrong page slices, inconsistent totals/pages).  
   Suggested fix: map `page/per_page` to `number/offset` in endpoint before calling manager.
   Status: **Fixed** (2026-02-14).  
   Resolution: `get_reviews()` and `get_listing_reviews()` now map `page/per_page` to `number/offset` before calling `apd_get_listing_reviews()`, with `offset = (page - 1) * per_page` (`src/Api/Endpoints/ReviewsEndpoint.php`).

3. Severity: **Medium**  
   File & line: `src/Api/Endpoints/ReviewsEndpoint.php:159`, `src/Api/Endpoints/ReviewsEndpoint.php:474`, `src/Review/ReviewManager.php:498`  
   What’s wrong: `author` filter is exposed in REST params but not applied by `ReviewManager::get_listing_reviews()`.  
   Why it matters: clients receive unfiltered results and may leak/consume wrong data.  
   Suggested fix: add author/user_id filtering in manager query args and corresponding tests.
   Status: **Fixed** (2026-02-14).  
   Resolution: `ReviewManager::get_listing_reviews()` now applies `author`/`user_id` as `user_id` in comment query args, and tests were added for endpoint passthrough plus manager query filtering (`src/Review/ReviewManager.php`, `tests/unit/Api/Endpoints/ReviewsEndpointTest.php`, `tests/unit/Review/ReviewManagerTest.php`).

4. Severity: **High**  
   File & line: `src/Search/SearchQuery.php:403`, `src/Search/SearchQuery.php:406`, `src/Search/SearchQuery.php:407`, `src/Search/SearchQuery.php:408`, `src/Api/AjaxHandler.php:94`  
   What’s wrong: `get_filtered_listings()` runs `new WP_Query()` before applying keyword/filters/orderby; filter classes mostly call `$query->set(...)` only.  
   Why it matters: AJAX/front-end filtered results can be silently wrong/unfiltered.  
   Suggested fix: build final query args first, then instantiate `WP_Query`; or rerun query after applying modifications.
   Status: **Fixed** (2026-02-14).  
   Resolution: `get_filtered_listings()` now builds final query vars first via a query-var collector (`build_filtered_query_args()`), then executes a single `new WP_Query( $final_query_args )`. Added a unit test that asserts keyword/filter/orderby mutations are present in final args before execution (`src/Search/SearchQuery.php`, `tests/unit/Search/SearchQueryTest.php`).

5. Severity: **High**  
   File & line: `src/Fields/FieldValidator.php:122`, `src/Fields/Types/NumberField.php:105`, `src/Fields/Types/DecimalField.php:110`, `src/Fields/Types/CurrencyField.php:139`, `src/Fields/AbstractFieldType.php:231`  
   What’s wrong: validation sanitizes first; empty numeric inputs become `0`/`0.0`, then required checks treat them as non-empty.  
   Why it matters: blank required numeric fields can be stored as zero (silent data corruption/semantic errors).  
   Suggested fix: run required-empty checks before sanitize for required fields, or preserve empty sentinel through sanitize.
   Status: **Fixed** (2026-02-14).  
   Resolution: `FieldValidator::validate_field()` now performs required-empty checks before sanitization, and `sanitize_field()` preserves empty sentinels for required fields to prevent coercion to numeric zero. Added regression tests for required `number`/`decimal`/`currency` empties and `process_fields()` sentinel preservation (`src/Fields/FieldValidator.php`, `tests/unit/Fields/FieldValidatorTest.php`).

6. Severity: **Medium**  
   File & line: `src/Api/Endpoints/InquiriesEndpoint.php:391`, `src/Api/Endpoints/InquiriesEndpoint.php:440`, `src/Contact/InquiryTracker.php:287`, `src/Contact/InquiryTracker.php:299`, `src/Contact/InquiryTracker.php:356`, `src/Contact/InquiryTracker.php:369`, `src/Api/Endpoints/InquiriesEndpoint.php:447`  
   What’s wrong: same pagination contract mismatch (`page/per_page` vs `number/offset`), plus listing total count ignores requested status filter.  
   Why it matters: broken pagination and incorrect `total/max_pages` metadata.  
   Suggested fix: normalize pagination args before tracker calls; compute totals with same filter args used for items.
   Status: **Fixed** (2026-02-14).  
   Resolution: `InquiriesEndpoint` now maps `page/per_page` to `number/offset` for both user and listing inquiry reads, and listing totals are now calculated with the requested status via `apd_get_listing_inquiry_count( $listing_id, $status )`. `InquiryTracker::get_listing_inquiry_count()` now supports status-aware counting for `read`/`unread` while preserving cached `all` counts (`src/Api/Endpoints/InquiriesEndpoint.php`, `src/Contact/InquiryTracker.php`, `includes/functions.php`, `tests/unit/Api/Endpoints/InquiriesEndpointTest.php`).

7. Severity: **Medium**  
   File & line: `src/Contact/ContactHandler.php:199`, `src/Contact/ContactHandler.php:212`, `src/Contact/ContactHandler.php:328`  
   What’s wrong: `get_userdata()` can return false, but `send_email()` requires `WP_User`.  
   Why it matters: contact submission can fatal with `TypeError` for orphaned/deleted authors.  
   Suggested fix: guard `!($owner instanceof \WP_User)` and return WP_Error before calling `send_email()`.
   Status: **Fixed** (2026-02-14).  
   Resolution: `ContactHandler::process()` now validates owner lookup with `instanceof \WP_User` and returns `WP_Error( 'listing_owner_not_found', ... )` before hooks/email send when the owner record is missing, preventing the `TypeError` path into `send_email()`. Added a unit regression test that simulates owner lookup failure and asserts no email/action dispatch (`src/Contact/ContactHandler.php`, `tests/unit/Contact/ContactHandlerTest.php`).

8. Severity: **Medium**  
   File & line: `src/User/Favorites.php:154`, `src/User/Favorites.php:166`, `src/User/Favorites.php:208`, `src/User/Favorites.php:220`  
   What’s wrong: user favorites list is updated via read-modify-write on usermeta (non-atomic).  
   Why it matters: concurrent toggles can lose updates and desync actual favorites list from expected state.  
   Suggested fix: use compare-and-swap retry logic on usermeta or single-row normalized favorites table with unique constraints.
   Status: **Fixed** (2026-02-14).  
   Resolution: replaced `add()`/`remove()` read-modify-write persistence with optimistic compare-and-swap retries over usermeta (`get_user_meta` + `update_user_meta(..., $prev)` with bounded retry and atomic create via `add_user_meta(..., true)`). Count/hook side effects now run only when the mutation actually changed favorites, preventing double increments/decrements during races. Added regression tests for CAS conflict retry and no-double-side-effects outcomes (`src/User/Favorites.php`, `tests/unit/User/FavoritesTest.php`).

9. Severity: **Medium**  
   File & line: `src/Frontend/Submission/SubmissionHandler.php:1349`, `src/Contact/ContactHandler.php:753`  
   What’s wrong: rate-limit identity trusts spoofable proxy headers (`X-Forwarded-For`, `X-Real-IP`) without trusted-proxy validation.  
   Why it matters: attacker can rotate spoofed IP headers to bypass limits.  
   Suggested fix: trust forwarded headers only behind known proxy config; otherwise use `REMOTE_ADDR` only.
   Status: **Fixed** (2026-02-14).  
   Resolution: both submission and contact rate-limit identity now default to `REMOTE_ADDR` and only honor forwarded headers when `REMOTE_ADDR` matches a trusted proxy list (exact IP or CIDR) via `apd_submission_trusted_proxies` / `apd_contact_trusted_proxies`. Added unit tests for trusted vs untrusted proxy behavior in identifier generation (`src/Frontend/Submission/SubmissionHandler.php`, `src/Contact/ContactHandler.php`, `tests/unit/Frontend/Submission/SpamProtectionTest.php`, `tests/unit/Contact/ContactHandlerTest.php`).

10. Severity: **Medium**  
    File & line: `src/Core/Plugin.php:434`, `src/Core/Plugin.php:464`, `src/Core/Plugin.php:473`, `src/Core/Plugin.php:489`  
    What’s wrong: cron expiration/expiring queries are hard-capped to 50 and rely on non-atomic “NOT EXISTS then update meta” flow.  
    Why it matters: backlog on busy sites; duplicate expiring emails possible under concurrent cron runs.  
    Suggested fix: process in paged loop until exhausted; add lock/transient guard around expiring notification batch.
    Status: **Fixed** (2026-02-14).  
    Resolution: `cron_check_expired_listings()` now uses a transient lock to prevent overlapping runs, processes expired and expiring-soon listings in repeat batches until exhausted, and reserves expiring notifications atomically with `add_post_meta(..., true)` before send to avoid duplicate warning emails under concurrency (with cleanup on failed send). Added cron unit tests for lock behavior and multi-batch processing (`src/Core/Plugin.php`, `tests/unit/Core/CronHandlersTest.php`, `tests/unit/Core/CronExpirationTest.php`).

11. Severity: **Medium**  
    File & line: `src/Contact/InquiryTracker.php:432`, `src/Contact/InquiryTracker.php:614`, `includes/functions.php:3158`  
    What’s wrong: several hot paths use `posts_per_page => -1` (unbounded queries).  
    Why it matters: memory/time blowups on large datasets.  
    Suggested fix: replace with count-optimized queries or bounded paging loops.
    Status: **Fixed** (2026-02-14).  
    Resolution: inquiry count/recalc paths now use count-optimized queries (`posts_per_page` set to `1` with `found_posts`) instead of unbounded fetches, and favorite listings retrieval now avoids unbounded `-1` queries by using bounded query args and chunked batched fetches for full-list retrieval (`src/Contact/InquiryTracker.php`, `includes/functions.php`).

---

**2) Test Quality Audit**

12. Severity: **High**  
    File & line: `tests/integration/RestApiTest.php:49`, `tests/integration/SubmissionTest.php:29`, `tests/integration/SearchQueryTest.php:29`  
    What’s wrong: 3 integration suites are placeholder-only (`markTestIncomplete`).  
    Why it matters: core runtime behavior (REST, submission, search) has little real WP integration safety net.  
    Suggested fix: implement high-value integration cases first: auth/permission, pagination boundaries, submission validation, search filters.

13. Severity: **High**  
    File & line: `tests/unit/Security/SqlInjectionTest.php:22`, `tests/unit/Security/OutputEscapingTest.php:22`, `tests/unit/Security/InputSanitizationTest.php:27`  
    What’s wrong: many “security” tests validate WP/stub helpers or class existence, not plugin attack surfaces.  
    Why it matters: high test count but weak regression detection for real vulnerabilities.  
    Suggested fix: rewrite to target plugin entry points and responses (REST endpoints, template output, query builders, permission callbacks).

14. Severity: **Medium**  
    File & line: `tests/unit/Api/Endpoints/ReviewsEndpointTest.php:151`, `tests/unit/Api/Endpoints/InquiriesEndpointTest.php:152`, `tests/unit/Api/Endpoints/ListingsEndpointTest.php:143`, `tests/unit/Api/Endpoints/FavoritesEndpointTest.php:134`  
    What’s wrong: route tests mostly assert `register_rest_route` calls then `assertTrue(true)`.  
    Why it matters: they don’t validate route args, permission callbacks, or route behavior.  
    Suggested fix: assert registered route config payload, callback names, and critical args/permissions.

15. Severity: **Medium**  
    File & line: `tests/unit/Search/SearchQueryTest.php:85`, `tests/unit/Api/Endpoints/ReviewsEndpointTest.php:266`, `tests/unit/Api/Endpoints/InquiriesEndpointTest.php:336`, `tests/unit/Frontend/Submission/SubmissionHandlerTest.php:22`  
    What’s wrong: missing tests for discovered failures (search filtered query path, reviews/inquiries pagination mapping, pending-review public leak, numeric-required-empty pipeline).  
    Why it matters: current suite misses exactly the regressions found above.  
    Suggested fix: add focused tests that fail on current behavior, then patch code.
    Status: **Fixed** (2026-02-14).  
    Resolution: added focused regression tests for all identified failure paths: filtered search query arg building, reviews/inquiries pagination mapping, pending-review visibility rules, and required-empty numeric validation pipeline (`tests/unit/Search/SearchQueryTest.php`, `tests/unit/Api/Endpoints/ReviewsEndpointTest.php`, `tests/unit/Api/Endpoints/InquiriesEndpointTest.php`, `tests/unit/Fields/FieldValidatorTest.php`, `tests/unit/Review/ReviewManagerTest.php`).

16. Severity: **Low**  
    File & line: `tests/unit/ExampleTest.php:24`, `tests/unit/ExampleTest.php:88`, `tests/unit/Core/ActivatorTest.php:75`  
    What’s wrong: tautological tests (`assertTrue(true)`), mostly validating harness, not business logic.  
    Why it matters: inflates counts without improving defect detection.  
    Suggested fix: keep one smoke test; remove or replace the rest with behavior assertions.

---

**3) Architectural Concerns**

17. Severity: **Medium**  
    File & line: `src/Search/SearchQuery.php:166`, `src/Search/SearchQuery.php:203`, `includes/functions.php:926`  
    What’s wrong: search logic reads `$_GET` directly and helper returns static instance with hidden state.  
    Why it matters: implicit global state hurts determinism/testability and complicates reuse outside request context.  
    Suggested fix: inject request/query params explicitly; avoid static helper state for mutable query behavior.
    Status: **Fixed** (2026-02-14).  
    Resolution: `SearchQuery` now consumes explicit request/query params (constructor, setter, and method-level override) instead of reading `$_GET` internally; filter/order/keyword logic and current-value helpers all use injected params. `apd_search_query()` no longer returns a static mutable instance and can accept explicit request params; AJAX filtering now passes request params explicitly into both listing query and active-filter resolution (`src/Search/SearchQuery.php`, `includes/functions.php`, `src/Api/AjaxHandler.php`, `src/Core/Plugin.php`, `tests/unit/Search/SearchQueryTest.php`).

18. Severity: **Medium**  
    File & line: `src/Core/Plugin.php:418`, `src/Email/EmailManager.php:146`, `src/Contact/ContactHandler.php:486`  
    What’s wrong: strong hidden coupling to global settings/functions (`apd_get_setting`, `get_option`) inside domain logic.  
    Why it matters: pushes tests toward heavy mocking and obscures dependencies.  
    Suggested fix: inject config/services into constructors for core flows, keep globals at composition boundary only.
    Status: **Fixed** (2026-02-14).  
    Resolution: introduced `APD\Core\Config` as an injected configuration service and routed option/setting reads through it in `Plugin`, `EmailManager`, and `ContactHandler`. Core composition now passes the shared config service into `ContactHandler::get_instance()` and `EmailManager::get_instance()` from `Plugin::init_hooks()`, while cron expiration now reads `expiration_days` through the injected service (`src/Core/Config.php`, `src/Core/Plugin.php`, `src/Email/EmailManager.php`, `src/Contact/ContactHandler.php`, `includes/functions.php`, `tests/unit/Email/EmailManagerTest.php`, `tests/unit/Contact/ContactHandlerTest.php`).

---

**4) Test Infrastructure Issues**

19. Severity: **High**  
    File & line: `tests/unit/bootstrap.php:60`, `src/Frontend/Submission/SubmissionHandler.php:272`, `src/Frontend/Submission/SubmissionHandler.php:573`  
    What’s wrong: unit `WP_Error` stub omits `merge_from()`, but production code calls it.  
    Why it matters: important error-merge paths are effectively untestable/misrepresented in unit environment.  
    Suggested fix: extend stub to match WP API used by code (at least `merge_from` and related behavior), or run those cases as integration tests.
    Status: **Fixed** (2026-02-14).  
    Resolution: extended the unit `WP_Error` stub with `merge_from()` and related API used in merge flows (`add_data`, `get_all_error_data`, `copy_errors`, `export_to`, `remove`) and added focused tests that execute both submission merge call sites (featured-image upload errors and custom-field validation errors), plus direct stub behavior tests (`tests/unit/bootstrap.php`, `tests/unit/Frontend/Submission/SubmissionHandlerTest.php`, `tests/unit/WPErrorStubTest.php`).

20. Severity: **Medium**  
    File & line: `tests/unit/UnitTestCase.php:59`, `tests/unit/Security/SecurityTestCase.php:107`  
    What’s wrong: stubs diverge from WP behavior (e.g., escaping/sanitization simplifications).  
    Why it matters: unit assertions can pass while real WP behavior differs.  
    Suggested fix: move behavior-sensitive assertions to integration tests; keep unit tests focused on plugin logic, not WP helper semantics.
    Status: **Fixed** (2026-02-14).  
    Resolution: clarified unit-test boundaries in base test cases and shifted security tests away from asserting mocked WordPress helper semantics to plugin behavior/contracts (delegation/source-contract checks, query-contract checks, nonce value-flow checks). Added a WordPress-backed integration test for sanitization/escaping semantics (`tests/integration/WordPressSanitizationBehaviorTest.php`) while keeping unit suites focused on plugin logic (`tests/unit/UnitTestCase.php`, `tests/unit/Security/SecurityTestCase.php`, `tests/unit/Security/InputSanitizationTest.php`, `tests/unit/Security/OutputEscapingTest.php`, `tests/unit/Security/SqlInjectionTest.php`, `tests/unit/Security/FileUploadSecurityTest.php`, `tests/unit/Security/NonceVerificationTest.php`).

21. Severity: **Low**  
    File & line: `tests/unit/UnitTestCase.php:34`, `tests/unit/UnitTestCase.php:45`  
    What’s wrong: none major here; Brain Monkey setup/teardown is generally correct.  
    Why it matters: lower risk of cross-test bleed from mock lifecycle itself.  
    Suggested fix: continue this pattern; focus cleanup work on singleton/static state and stub parity.

22. Severity: **Medium**  
    File & line: `tests/bootstrap.php:31`, `tests/TestCase.php:19`, `tests/integration/RestApiTest.php:49`  
    What’s wrong: integration framework is correctly WP-backed, but coverage is hollow where most needed.  
    Why it matters: “integration tests exist” but critical API/submission/search paths are still effectively unverified.  
    Suggested fix: prioritize replacing incomplete integration tests with a minimal but real critical-path matrix.

---

Assumptions: this review is static analysis of current code/tests; I did not run the full PHPUnit/Playwright suites in this pass.
